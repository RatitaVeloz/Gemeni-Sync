# Style Amenities Dropdown

The user wants the amenities dropdown to look like the standard select inputs used for other filters.

## User Review Required
> [!NOTE]
> I will change the text "Seleccionar amenidades" to "Cualquiera" to match the other filters as requested ("parece algo repetido").

## Proposed Changes

### Theme: hello-elementor-child

#### [MODIFY] [inc/shortcodes-filters.php](file:///c:/xampp/htdocs/ocean/wp-content/themes/hello-elementor-child/inc/shortcodes-filters.php)
*   Change the summary text from `Seleccionar amenidades` to `Cualquiera`.

#### [MODIFY] [style.css](file:///c:/xampp/htdocs/ocean/wp-content/themes/hello-elementor-child/style.css)
*   Style `.amenities-summary` to match `.filter-select`:
    *   Border, radius, background, font size/color.
    *   Add the dropdown arrow using `::after` or background image to mimic the select arrow.
    *   Ensure the dropdown content (`.amenities-checkboxes`) is positioned correctly (it's inside a `<details>` tag, so standard behavior applies, but might need some padding/border tweaks).

## Verification Plan

### Manual Verification
1.  **Visual**: Check that the "Amenidades" dropdown looks identical to the "Habitaciones" or "Baños" dropdowns.
2.  **Text**: Verify the label says "Amenidades" and the box says "Cualquiera".
3.  **Interaction**: Click to open and verify the checkboxes appear correctly.
