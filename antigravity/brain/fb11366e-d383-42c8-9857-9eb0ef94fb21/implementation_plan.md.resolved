# Refine to Full Width Mobile Modal

The user wants to remove the transparent margins around the modal, making it a full-width bottom sheet with a white background.

## User Review Required
> [!NOTE]
> I will remove the side borders and side padding. The modal will now span the full width of the screen.

## Proposed Changes

### Theme: hello-elementor-child

#### [MODIFY] [style.css](file:///c:/xampp/htdocs/ocean/wp-content/themes/hello-elementor-child/style.css)
Update the following CSS rules:

1.  **Modal Content (`.op-filters-modal-content`)**:
    *   `padding: 0 !important`.
    *   `margin-bottom: 0 !important`.
    *   `max-width: 100% !important`.

2.  **Sidebar Container (`.op-sidebar-mobile`)**:
    *   `border-radius: 16px 16px 0 0 !important`.
    *   `border: none !important`.
    *   `border-top: 1px solid #B0B8C7 !important`.

## Verification Plan

### Manual Verification
1.  **Mobile**: Open modal.
2.  **Edges**: Verify the white background touches the left, right, and bottom edges of the screen.
3.  **Border**: Verify there is only a top border and top rounded corners.
